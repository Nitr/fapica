#class MainController extends Spine.Controller

 # className: 'main'

 # constructor: ->    
  #  super
    
  #  @start = new StartController
  #  @kpp = new KppController
    #@edit = new PagesEdit
    #@item = new PagesItem
    
  #  new Spine.Manager(@start, @kpp)
    
  #  @append(@start, @kpp)
    
  #  @routes
     # '/pages/:id/edit': (params) ->
     #   @edit.active(params)
     # '/pages/:id': (params) ->
     #   @item.active(params)
  #    '/': (params) -> 
  #      @start.active(params)

  #  Spine.Route.setup()
    
  #  @navigate '/'
    

#window.MainController = MainController